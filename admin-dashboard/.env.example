# Backend API URL (no trailing /api - it's already in every endpoint path)
# Leave EMPTY for same-origin (backend serves admin build)
# Set to backend origin ONLY when admin dashboard is on a different origin
#
# Development:   http://localhost:5000
# Render single: (leave empty - same origin)
# Render split:  https://prakurthi-bags.onrender.com
VITE_API_URL=
