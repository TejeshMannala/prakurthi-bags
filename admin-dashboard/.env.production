# Production build — loaded by Vite when mode=production (vite build).
# Empty VITE_API_URL so the admin dashboard uses window.location.origin
# (same-origin, since the admin is served by the backend Express server).
VITE_API_URL=
