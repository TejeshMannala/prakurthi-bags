{
  "name": "prakruthi-bags",
  "private": true,
  "scripts": {
    "install-all": "npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build && npm --prefix admin-dashboard install && npm --prefix admin-dashboard run build",
    "build": "npm --prefix backend install",
    "start": "node backend/server.js"
  }
}
