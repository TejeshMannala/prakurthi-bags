{
  "name": "prakruthi-bags",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix frontend install && npm --prefix frontend run build && npm --prefix admin-dashboard install && npm --prefix admin-dashboard run build",
    "build": "npm --prefix backend install && npm --prefix frontend install && npm --prefix frontend run build && npm --prefix admin-dashboard install && npm --prefix admin-dashboard run build",
    "start": "node backend/server.js"
  }
}
